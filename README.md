# Projeto teste em Robot Framework Zé Palheta

Validar cenários web e api.

# Subir projeto

 - [x] docker-compose up -d
 - [x] Para iniciar o setup da api : docker exec -it zepalheta-api sh /home/node/api/setup.sh
